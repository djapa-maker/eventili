{% extends 'baseAdmin.html.twig'%}

{% block body %}
	{% include 'baseAdmin.html.twig' %}
	<div class="page-wrapper">
		<div class="page-content">


			<div class="card">
				<div class="card-body">
					
							{{ include('templates_back/personne/_form.html.twig', {'button_label': 'Modifier'}) }}

						
				</div>
			</div>
		</div>
	</div>
{% endblock %}
