<form method="post" action="{{ path('app_imagepers_delete', {'idImp': imageper.idImp}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ imageper.idImp) }}">
   <button class ="btn btn-danger"">Supprimer</button>
</form>
