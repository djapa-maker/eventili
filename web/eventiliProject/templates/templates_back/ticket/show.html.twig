{% extends 'baseAdmin.html.twig'%}
{% block title %}Ticket{% endblock %}

{% block body %}
    <h1>Ticket</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdTick</th>
                <td>{{ ticket.idTick }}</td>
            </tr>
            <tr>
                <th>NbTick</th>
                <td>{{ ticket.nbTick }}</td>
            </tr>
            <tr>
                <th>PrixTick</th>
                <td>{{ ticket.prixTick }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ ticket.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ticket_index') }}">back to list</a>

    <a href="{{ path('app_ticket_edit', {'idTick': ticket.idTick}) }}">edit</a>

    {{ include('templates_back/ticket/_delete_form.html.twig') }}
{% endblock %}

