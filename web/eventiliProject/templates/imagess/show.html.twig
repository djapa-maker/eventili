{% extends 'baseAdmin.html.twig' %}

{% block title %}Imagess{% endblock %}

{% block body %}
    <h1>Imagess</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idimgss</th>
                <td>{{ imagess.idimgss }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ imagess.img }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_imagess_index') }}">back to list</a>

    <a href="{{ path('app_imagess_edit', {'idimgss': imagess.idimgss}) }}">edit</a>

    {{ include('imagess/_delete_form.html.twig') }}
{% endblock %}
