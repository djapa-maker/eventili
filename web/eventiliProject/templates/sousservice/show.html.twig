{% extends 'base.html.twig' %}

{% block title %}Sousservice{% endblock %}

{% block body %}
    <h1>Sousservice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sousservice.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ sousservice.nom }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ sousservice.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ sousservice.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ sousservice.prix }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ sousservice.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sousservice_index') }}">back to list</a>

    <a href="{{ path('app_sousservice_edit', {'id': sousservice.id}) }}">edit</a>

    {{ include('sousservice/_delete_form.html.twig') }}
{% endblock %}
