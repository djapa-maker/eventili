
  <style>.btn-delete {
    display: inline-block;
    padding: 10px;
    background-color: #f44336;
    color: #fff;
    text-decoration: none;
    border-radius: 5px;
}

.btn-delete:hover {
    background-color: #cc0000;
}
    </style>
<form method="post" action="{{ path('app_sponsoring_delete', {'id_sponso': sponsoring.idSponso}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sponsoring.idSponso) }}">
    <button class="btn-delete">Delete</button>
</form>
