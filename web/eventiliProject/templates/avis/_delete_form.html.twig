<form method="post" action="{{ path('app_avis_delete', {'idAv': avi.idAv}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avi.idAv) }}">
    <button class="btn">Delete</button>
</form>
