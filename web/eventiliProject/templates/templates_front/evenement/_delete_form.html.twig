<form method="post" action="{{ path('app_evenement_delete', {'idEv': evenement.idEv}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.idEv) }}">
    <button class="btn">Delete</button>
</form>
