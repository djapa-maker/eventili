<!DOCTYPE html>

<title>Imgev</title>

{% block body %}
    <h1>Imgev</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdImgev</th>
                <td>{{ imgev.idImgev }}</td>
            </tr>
            <tr>
                <th>Imagee</th>
                <td>{{ imgev.imagee }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_imgev_index') }}">back to list</a>

    <a href="{{ path('app_imgev_edit', {'idImgev': imgev.idImgev}) }}">edit</a>

    {{ include('imgev/_delete_form.html.twig') }}
{% endblock %}
