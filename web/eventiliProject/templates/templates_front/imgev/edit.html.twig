<!DOCTYPE html>

<title>Edit Imgev</title>

{% block body %}
    <h1>Edit Imgev</h1>

    {{ include('imgev/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_imgev_index') }}">back to list</a>

    {{ include('imgev/_delete_form.html.twig') }}
{% endblock %}
